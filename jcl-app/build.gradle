description = 'JCL-App'

apply plugin: 'application'
mainClassName = "jcl.JCL"

String GROOVY_VERSION = '2.1.9';

dependencies {
	compile project(':jcl-reader')
	compile project(':jcl-compiler')

	runtime group: 'org.codehaus.groovy', name: 'groovy', version: "$GROOVY_VERSION"
}
